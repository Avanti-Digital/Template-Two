---
import './TextLink.css';
interface Props {
  href: string;
  label: string;
}
const { href, label } = Astro.props;
---
<a class="text-link" href={href}><span>{label}</span><span aria-hidden="true">↗</span></a>
